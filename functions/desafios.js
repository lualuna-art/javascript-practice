//COD3R DESAFIOS

// --- DESAFIOS
// 1. somar(3)(4)(5)

const somar2 = (n1) => {
    return num2 = (n2) => {
        return num3 = (n3) => n1 + n2 + n3
    }
}
console.log(somar2(10)(10)(10))



// 2.  calcular(3)(7)(fn) dois numero e uma função

const total = (a,b) => a + b

let calcular = (num1) => {
    return segundoParam = (num2) => {
        return terceiroParam = (fn) => {
            return fn(num1,num2)
        }
    }
}
console.log(calcular(3)(3)(total))